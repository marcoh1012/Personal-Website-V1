// const $terminal = $("#introBox")
// $terminal.ready(() => { typewritter(0) })

// const info = ["> Hello World! ", "Im, Marco Herrera", "Im a Sotfware Engineer"]
// const $lines = [$(".code"), $(".name"), $(".subTitle")]

// function typewritter(i) {
//     if (i < info.length) {
//         typeString(info[i], $lines[i], 0, function() { typewritter(i + 1) })
//     }

// }

// function typeString(str, line, i, callback) {
//     if (i < str.length) {
//         line.append(str[i])
//         setTimeout(() => { typeString(str, line, i + 1, callback) }, 200)
//     } else {
//         setTimeout(callback, 500)
//     }

// }